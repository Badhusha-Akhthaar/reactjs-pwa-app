_schema-version: "3.2"
ID: salesorderpwa
version: 0.0.1
modules:
- name: salesorder-pwa
  type: nodejs
  path: .
  parameters:
    disk-quota: 256M
    memory: 256M
    host: salesorder-pwa-dev

build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install


